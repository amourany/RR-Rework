package test;

import static org.junit.Assert.assertEquals;
import game.Game;

import org.junit.Test;

public class GameTests {

	Game m=new Game();
	
	@Test
	public void testInit() {
		assertEquals("Robots", 0, m.getRobots().size());
		assertEquals("Currentmove", 1, m.getCurrentRound());
		//assertNotNull("Players", m.getPlayers());
		/*
		assertNull("Previous Move", m.getPreviousMove());
		assertNull("Next Move", m.getNextMove());*/
	} 

}
